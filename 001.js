// Generated by CoffeeScript 1.9.2
(function() {
  var R, getMultiples, multiples, sequence;

  R = require('ramda');

  sequence = function(first, last) {
    return R.unfold((function(n) {
      if (n > last) {
        return false;
      } else {
        return [n, n + 1];
      }
    }), first);
  };

  getMultiples = R.compose(R.reduce((function(x, y) {
    return x + y;
  }), 0), R.filter(function(n) {
    return n % 3 === 0 || n % 5 === 0;
  }));

  multiples = getMultiples(sequence(1, 999));

  console.log(multiples);

}).call(this);
