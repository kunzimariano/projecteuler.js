// Generated by CoffeeScript 1.9.2
(function() {
  var R, c, s, sequence;

  R = require('ramda');

  s = function(first, last) {
    var f;
    f = function(n) {
      if (n > last) {
        return false;
      } else {
        return [n, n + 1];
      }
    };
    return R.unfold(f, first);
  };

  sequence = R.curry(s);

  c = R.compose(R.reduce((function(x, y) {
    return x + y;
  }), 0), R.filter(function(n) {
    return n % 3 === 0 || n % 5 === 0;
  }));

  console.log(c(sequence(1, 999)));

}).call(this);
